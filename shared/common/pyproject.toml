[project]
name = "deeplearning-ai-common"
version = "0.1.0"
description = "Shared utilities across DeepLearning.AI courses"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
