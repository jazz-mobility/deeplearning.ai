[project]
name = "agentic-ai-module-03"
version = "0.1.0"
description = "Module 3: Tool Use Design Pattern"
requires-python = ">=3.10"
dependencies = [
    "agentic-ai",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
