[project]
name = "agentic-ai-module-01"
version = "0.1.0"
description = "Module 1: Introduction to Agentic Workflows"
requires-python = ">=3.10"
dependencies = [
    "agentic-ai",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
