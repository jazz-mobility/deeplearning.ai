[project]
name = "agentic-ai-module-02"
version = "0.1.0"
description = "Module 2: Reflection Design Pattern"
requires-python = ">=3.10"
dependencies = [
    "agentic-ai",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
